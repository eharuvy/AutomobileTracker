# Example .env for AutomobileTracker
# Copy this file to `.env` and fill in your local credentials.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=misc
DB_USER=fred
DB_PASSWORD=zap

# Optional: when deploying to a host, set these values in the host's environment